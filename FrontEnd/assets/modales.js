async function delete(workId) {
    //workId=l'id du projet
     fetch("http://localhost:5678/api/works/{workId})
    const resultat = await reponse.json();//On transforme la réponse en format json()
}
